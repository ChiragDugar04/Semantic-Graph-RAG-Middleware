# RAG_Middleware
It is a local AI middleware designed to bridge the gap between natural language and structured relational databases. Unlike traditional RAG systems that rely on vector-only search, this project implements an Agentic Pipeline that classifies user intent, extracts structured parameters, and executes precise SQL queries against a MySQL backend.
